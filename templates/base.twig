<!doctype html>
<html lang="">

    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="shortcut icon" href="{{ base_url() }}/images/favicon.ico" type="image/x-icon" />
        <title>{{ title }}</title>
        <link href="https://fonts.googleapis.com/css?family=Merriweather|Alex+Brush|Dancing+Script|Neucha" rel="stylesheet">
        <!--Import Google Icon Font-->
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <!--Import materialize.css-->
        <link type="text/css" rel="stylesheet" href="{{ base_url() }}/styles/materialize.min.css"/>
        <link type="text/css" rel="stylesheet" href="{{ base_url() }}/styles/font-awesome.min.css"/>

        <link type="text/css" rel="stylesheet" href="{{ base_url() }}/styles/jssocials.css"/>
        <link type="text/css" rel="stylesheet" href="{{ base_url() }}/styles/jssocials-theme-flat.css"/>
        <!-- Custom styles -->
        <link type="text/css" rel="stylesheet" href="{{ base_url() }}/styles/main.css"/>

    </head>

    <body>
        {% include 'partials/header.twig' %}
        <main id="content">
            {% include 'partials/flash.twig' %}
            {% block content %}{% endblock %}
        </main>
        {% include 'partials/footer.twig' %}

        <!-- remove bottom add to build-->

        <!-- remove top-->
        <!-- build:js scripts/main.min.js -->
        <script type="text/javascript" src="{{ base_url() }}/scripts/jquery-2.1.1.min.js"></script>
        <script type="text/javascript" src="{{ base_url() }}/scripts/materialize.min.js"></script>
        <script type="text/javascript" src="{{ base_url() }}/scripts/jssocials.min.js"></script>
        <script type="text/javascript" src="{{ base_url() }}/scripts/dom-i18n.min.js"></script>
        <script type="text/javascript" src="{{ base_url() }}/scripts/store2.js"></script>
        <script type="text/javascript" src="{{ base_url() }}/scripts/main.js"></script>

        <!-- endbuild -->

        <!-- Google Analytics: change UA-XXXXX-X to be your site's ID -->
        <!-- <script>
    (function(i, s, o, g, r, a, m) {
      i['GoogleAnalyticsObject'] = r;
      i[r] = i[r] || function() {
        (i[r].q = i[r].q || []).push(arguments)
      }, i[r].l = 1 * new Date();
      a = s.createElement(o),
        m = s.getElementsByTagName(o)[0];
      a.async = 1;
      a.src = g;
      m.parentNode.insertBefore(a, m)
    })(window, document, 'script', '//www.google-analytics.com/analytics.js', 'ga');
    ga('create', 'UA-XXXXX-X', 'auto');
    ga('send', 'pageview');
  </script> -->
    </body>

</html>
